{
  "name": "@sap-cloud-sdk/currency-conversion-data-adapter",
  "version": "0.1.1",
  "description": "Data adapter for the SAP Cloud SDK currency conversion library.",
  "main": "./build/index.js",
  "scripts": {
    "test": "jest",
    "compile": "yarn tsc -b",
    "prepare": "yarn compile",
    "coverage": "jest --coverage",
    "lint": "yarn eslint --ext .ts .",
    "lint:fix": "yarn lint --fix",
    "lint:fix-cached": "yarn lint:fix --cache"
  },
  "keywords": [
    "currency"
  ],
  "files": [
    "build/**/*.js",
    "build/**/*.json",
    "build/**/*.ts"
  ],
  "publishConfig": {
    "access": "public"
  },
  "author": "SAP",
  "license": "Apache-2.0",
  "types": "build/index.d.ts",
  "devDependencies": {
    "@types/jest": "^26.0.13",
    "@types/node": "^16.4.3",
    "chai": "4.3.1",
    "chai-as-promised": "7.1.1",
    "chai-subset": "1.6.0",
    "eslint": "^7.9.0",
    "jest": "^26.6.3",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.3.0",
    "ts-node": "^10.2.1",
    "tslint": "^6.1.3",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@sap-cloud-sdk/core": "^1.32.1",
    "@sap-cloud-sdk/currency-conversion-models": "^0.1.1",
    "@sap-cloud-sdk/util": "^1.32.1",
    "@sap/cds": "4.3.0",
    "@sap/hana-client": "^2.6.58",
    "sqlite3": "5.0.2"
  }
}
