/* Copyright (c) 2021 SAP SE or an SAP affiliate company. All rights reserved. */
const { createLogger } = require('@sap-cloud-sdk/util');

const logger = createLogger('integration-objects');

module.exports = { logger };
